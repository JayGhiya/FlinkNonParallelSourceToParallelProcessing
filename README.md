# FlinkNonParallelSourceToParallelProcessing
This project demonstrates connecting to tcp socket stream as a source and then splitting the data across multiple flink partitions for parallel processing,.
